@using FashionNet.Utilidades
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - FashionNet</title>
     @*Google Fonts*@
    <link href="https://fonts.googleapis.com/css?family=Open+Sans:300,400|Source+Code+Pro:700,900&display=swap" rel="stylesheet">

    @*CSS Libraries*@
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.10.0/css/all.min.css" rel="stylesheet">
</head>
<body>
    @*<header>
            <nav class="navbar navbar-expand-sm navbar-toggleable-sm navbar-light bg-white border-bottom box-shadow mb-3">
                <div class="container">
                    <a class="navbar-brand" asp-area="" asp-controller="Home" asp-action="Index">FashionNet</a>
                    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target=".navbar-collapse" aria-controls="navbarSupportedContent"
                            aria-expanded="false" aria-label="Toggle navigation">
                        <span class="navbar-toggler-icon"></span>
                    </button>
                    <div class="navbar-collapse collapse d-sm-inline-flex justify-content-between">
                        <ul class="nav navbar-nav">
                            @if (User.IsInRole(CNT.Admin))
                            {
                                <li class="dropdown">
                                    <a class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Componentes <span class="caret"></span></a>
                                    <ul class="dropdown-menu">
                                        <a class="nav-link text-dark" asp-area="Admin" asp-controller="Categorias" asp-action="Index">Categorias</a>
                                        <a class="nav-link text-dark" asp-area="Admin" asp-controller="SubCategorias" asp-action="Index">Sub-Categorias</a>
                                        <a class="nav-link text-dark" asp-area="Admin" asp-controller="Marcas" asp-action="Index">Marcas</a>
                                        <a class="nav-link text-dark" asp-area="Admin" asp-controller="Usuarios" asp-action="ListadoUsuarios">Usuarios</a>
                                        <a class="nav-link text-dark" asp-area="Admin" asp-controller="Productos" asp-action="Index">Productos</a>
                                    </ul>
                                </li>
                            }
                        </ul>
                    </div>
                </div>
            </nav>
        </header>*@
    <partial name="_Header" />

    @RenderBody()

    <!--<partial name="_Footer" />-->
    <!-- Back to Top -->
    <a href="#" class="back-to-top"><i class="fa fa-chevron-up"></i></a>



    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
